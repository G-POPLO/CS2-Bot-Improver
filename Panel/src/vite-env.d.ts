/// <reference types="vite/client" />

/** Injected by Vite's `define` from Panel/package.json — the single source of
 *  truth for the app version, shared with the title bar, the update checker and
 *  the Inno Setup installer. */
declare const __APP_VERSION__: string;
